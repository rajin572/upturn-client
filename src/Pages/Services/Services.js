import React from 'react';
import useTitle from '../../hook/useTitle';

const Services = () => {
    useTitle('services')
    return (
        <div>
            <h2>This is service part</h2>
        </div>
    );
};

export default Services;